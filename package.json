{ "name": "BFileUpload",
  "version": "1.0.0",
  "description": "大文件切片上传",
  "scripts": {
    "bootstrap": "npm-run-all --serial dev:*",
    "dev:vite": "cd vite-client && npm run dev",
    "dev:nest": "cd nest-server && npm run dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
